package advent

import (
	"fmt"
	"testing"

	"github.com/stretchr/testify/assert"
)

func DayNNP1(input string) int {
	tot := 0
	for _, l := range Lines(input) {
		fmt.Println(l)
	}
	return tot
}

func DayNNP2(input string) int {
	return 0
}

func TestDayNN_Ex1(t *testing.T) {
	assert.Equal(t, DayNNP1(exDNN), 42)
}

func TestDayNN_Ex2(t *testing.T) {
	assert.Equal(t, DayNNP2(exDNN), 42)
}

func TestDayNN_P1_Actual(t *testing.T) {
	fmt.Printf("Day NN Part 1: %d\n", DayNNP1(inputDNN))
}

func TestDayNN_P2_Actual(t *testing.T) {
	fmt.Printf("Day NN Part 2: %d\n", DayNNP2(inputDNN))
}

const exDNN = ``

const inputDNN = ``
